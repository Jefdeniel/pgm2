// const fs = require("fs"); // file system module

import fs from "fs"; // Dit doet exact hetzelfde als hierboven,

console.log(fs);

// via de command npm init kan je een package.json aanmaken

// via de command npm install express --save kan je express installeren

// via de command npm install nodemon --save-dev kan je nodemon installeren

// via de command npm install ejs --save kan je ejs installeren

// via de command npm install body-parser --save kan je body-parser installeren
